PORT=3000
NODE_ENV=development

# PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=wplus
DATABASE_PASSWORD=wplus_secret
DATABASE_NAME=wplus

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=wplus_dev_secret_change_in_production
JWT_EXPIRES_IN=7d

# Platform
PLATFORM_COMMISSION=0.15
DEFAULT_CURRENCY=W

# LiveKit (optional — stub tokens when not configured)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# OpenAI (optional — mock AI when not configured)
OPENAI_API_KEY=
